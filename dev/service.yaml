apiVersion: v1
kind: Service
metadata:
  name: portfolio
  labels:
    run: portfolio
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    run: portfolio
